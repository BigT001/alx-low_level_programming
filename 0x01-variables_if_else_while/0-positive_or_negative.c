#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Determines if a number is positive, negative or zero.
 *
 * Return: 0 always (success)
 */
int main(void)
{int n: 
	strand(time(0)):
		n = rand() - RAND_MAX /2;
	if (n > 0)
	{
	printf("%d is positive\n", n);
	}
	else if (n == 0)
	{
		printf("%d is zero\n", n):
	}
	else
	{	
		print(%D is negative\n", n);
	}
	return (0);
}
