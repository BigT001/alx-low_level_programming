#include "main.h"

/**
 * _isupper - Uppercase returns 1 if not return 0
 * @c: this is the char to check
 * Return: 0
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
