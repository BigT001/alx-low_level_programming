#include "main.h"
#include <stdio.h>

/**
 * mul - multiples two integers
 * @a: - the first integer
 * @b: - the second integer
 * Return: 0 always
 */

int mul(int a, int b)
{
int result = a * b;
return (result);
}
